import React from "react";
import { Grid } from "@material-ui/core";
import * as S from "./styles"

function Header() {
  return (
      <S.Header container alignContent="center">
        <Grid item>A</Grid>
      </S.Header>
  );
}

export default Header;
